**PriorityBlockingQueue 阻塞队列使用**

#### # 通常的阻塞队列有哪几种，特点是什么？

- `ArrayBlockQueue`：基于数组实现的有界的FIFO(先进先出)阻塞队列。
- `LinkedBlockQueue`：基于链表实现的无界的FIFO(先进先出)阻塞队列。
- `SynchronousQueue`：内部没有任何缓存的阻塞队列。
- `PriorityBlockingQueue`：具有优先级的无限阻塞队列。

 